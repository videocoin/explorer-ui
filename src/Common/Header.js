import React from 'react';
import { Logo } from 'ui-kit';

const Header = () => {
  return (
    <div>
      <Logo type="colorWhite" width={171} />
    </div>
  );
};

Header.defaultProps = {};

export default Header;
